
Input domain: shoot.example.com
{subdomain} => shoot
{domain} => example.com
{x} is the category
{x1} is any category other than x

```
{x}.{subdomain}.{domain}
{x}-{subdomain}.{domain}
{x}-{x1}-{subdomain}.{domain}
{subdomain}-{x}.{domain}
```
